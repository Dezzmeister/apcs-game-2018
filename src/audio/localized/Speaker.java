package audio.localized;

import render.core.Camera;
import render.math.Vector2;

public class Speaker {/*
	private Vector2 pos;
	private Camera camera;
	private AdvancedPlayer audio;
	private float fadeRadius;
	
	public Speaker(float x, float y, int radius, Camera _camera, AdvancedPlayer _audio) {
		pos = new Vector2(x,y);
		fadeRadius = radius;
		camera = _camera;
		audio = _audio;
	}
	
	public Speaker(Vector2 _pos, int radius, Camera _camera, AdvancedPlayer _audio) {
		pos = _pos;
		fadeRadius = radius;
		camera = _camera;
		audio = _audio;
	}
	
	public void on() {
		
	}*/
}
